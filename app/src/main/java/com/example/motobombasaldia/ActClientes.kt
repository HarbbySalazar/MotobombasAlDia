package com.example.motobombasaldia

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.ArrayAdapter
import android.widget.Spinner

class ActClientes : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_registro)
      /*  cmbIdentificaciones = findViewById(R.id.cbTipoUsuario)
        val adaptador = ArrayAdapter.createFromResource(this, R.array.tiposIdentificacion,android.R.layout.activity_list_item)
        adaptador.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item)
        cmbIdentificaciones.adapter = adaptador*/

    }
}